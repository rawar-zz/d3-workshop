1. Pig Latin Skript ansehen

2. $ pig order-by-country.pig ausführen

3. grunt> fs -ls -h

4. ./get-ordered-by-country.sh ausführen (Daten ins lokale Dateisystem holen)

5. $ python create-flare.py ordered_by_country.tsv > ordered_by_country.json

6, JSON-Datei ansehen, Aufbau der Hierarchie verstehen

7. Das führende [ und das letzte ] löschen

8. radial-tree-graph.html aufrufen und nachvollziehen 
